# 网上综合书店销售数据库项目
## 第一阶段：表结构草案（3NF）
> 文档用途：作为 `create_tables.sql` 的结构依据  
> 说明：本草案为逻辑到物理设计的过渡版本，字段类型可在 MySQL/PostgreSQL 落地时微调
---
## 1. 设计原则
- 满足第三范式（3NF），避免冗余与更新异常
- 主键统一 `xxx_id`
- 外键字段与被引用主键同名
- 多对多关系使用中间表
- 显式定义 `NOT NULL` / `UNIQUE` / `CHECK` / `FOREIGN KEY`
---
## 2. 表清单总览
| 模块 | 表名 | 说明 |
|---|---|---|
| 门店 | `store` | 门店信息 |
| 供应链 | `supplier` | 供应商信息 |
| 供应链 | `supplier_product` | 供货关系（中间表） |
| 商品 | `category` | 商品分类 |
| 商品 | `product` | 商品父实体 |
| 图书 | `book` | 图书子实体 |
| 图书 | `publisher` | 出版社 |
| 图书 | `author` | 作者 |
| 图书 | `translator` | 译者 |
| 图书 | `book_author` | 图书-作者关系 |
| 图书 | `book_translator` | 图书-译者关系 |
| 客户 | `customer` | 客户 |
| 客户 | `member` | 会员子实体 |
| 销售 | `sale` | 销售主单 |
| 销售 | `sale_item` | 销售明细 |
---
## 3. 表结构明细（字段级草案）
## 3.1 `store`（门店）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `store_id` | PK | 门店主键 |
| `store_name` | NOT NULL, UNIQUE | 门店名称 |
| `city` | NOT NULL | 城市 |
| `address` | NOT NULL | 地址 |
| `phone` | UNIQUE | 联系电话 |
| `created_at` | NOT NULL | 创建时间 |
---
## 3.2 `supplier`（供应商）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `supplier_id` | PK | 供应商主键 |
| `supplier_name` | NOT NULL, UNIQUE | 供应商名称 |
| `contact_name` |  | 联系人 |
| `phone` |  | 联系电话 |
| `email` | UNIQUE | 邮箱 |
| `status` | NOT NULL | 状态（如 ACTIVE/INACTIVE） |
---
## 3.3 `category`（分类）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `category_id` | PK | 分类主键 |
| `category_name` | NOT NULL, UNIQUE | 分类名称 |
| `parent_category_id` | FK -> `category.category_id`（可空） | 父分类（支持层级） |
---
## 3.4 `product`（商品父表）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `product_id` | PK | 商品主键 |
| `product_name` | NOT NULL | 商品名称 |
| `category_id` | NOT NULL, FK -> `category.category_id` | 所属分类 |
| `unit_price` | NOT NULL, CHECK (`unit_price` > 0) | 零售价 |
| `cost_price` | CHECK (`cost_price` >= 0) | 成本价 |
| `barcode` | UNIQUE | 条码 |
| `status` | NOT NULL | 状态 |
| `created_at` | NOT NULL | 创建时间 |
---
## 3.5 `publisher`（出版社）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `publisher_id` | PK | 出版社主键 |
| `publisher_name` | NOT NULL, UNIQUE | 出版社名称 |
| `country` |  | 国家/地区 |
| `website` |  | 官网 |
---
## 3.6 `book`（图书子表）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `product_id` | PK, FK -> `product.product_id` | 与商品一一对应 |
| `isbn` | NOT NULL, UNIQUE | 国际标准书号 |
| `publisher_id` | NOT NULL, FK -> `publisher.publisher_id` | 出版社 |
| `publish_date` |  | 出版日期 |
| `edition` |  | 版次 |
| `language` |  | 语言 |
| `page_count` | CHECK (`page_count` > 0) | 页数 |
---
## 3.7 `author`（作者）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `author_id` | PK | 作者主键 |
| `author_name` | NOT NULL | 作者名 |
| `country` |  | 国家/地区 |
---
## 3.8 `translator`（译者）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `translator_id` | PK | 译者主键 |
| `translator_name` | NOT NULL | 译者名 |
| `country` |  | 国家/地区 |
---
## 3.9 `book_author`（图书-作者中间表）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `product_id` | FK -> `book.product_id` | 图书ID |
| `author_id` | FK -> `author.author_id` | 作者ID |
| `author_order` |  | 作者顺序 |
| 复合主键 | PK (`product_id`, `author_id`) | 防重复关联 |
---
## 3.10 `book_translator`（图书-译者中间表）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `product_id` | FK -> `book.product_id` | 图书ID |
| `translator_id` | FK -> `translator.translator_id` | 译者ID |
| 复合主键 | PK (`product_id`, `translator_id`) | 防重复关联 |
---
## 3.11 `supplier_product`（供应商-商品中间表）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `supplier_id` | FK -> `supplier.supplier_id` | 供应商ID |
| `product_id` | FK -> `product.product_id` | 商品ID |
| `supply_price` | NOT NULL, CHECK (`supply_price` > 0) | 供货价 |
| `min_order_qty` | CHECK (`min_order_qty` > 0) | 起订量 |
| `is_primary` | NOT NULL, DEFAULT false | 是否主供 |
| 复合主键 | PK (`supplier_id`, `product_id`) | 防重复供货关系 |
---
## 3.12 `customer`（客户）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `customer_id` | PK | 客户主键 |
| `customer_name` | NOT NULL | 客户姓名 |
| `phone` | UNIQUE | 手机号 |
| `email` | UNIQUE | 邮箱 |
| `register_time` | NOT NULL | 注册时间 |
| `status` | NOT NULL | 状态 |
---
## 3.13 `member`（会员子表）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `customer_id` | PK, FK -> `customer.customer_id` | 与客户一一对应 |
| `member_no` | NOT NULL, UNIQUE | 会员编号 |
| `level` | NOT NULL | 会员等级 |
| `points` | NOT NULL, DEFAULT 0, CHECK (`points` >= 0) | 积分 |
| `join_date` | NOT NULL | 入会日期 |
---
## 3.14 `sale`（销售主单）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `sale_id` | PK | 销售单主键 |
| `store_id` | NOT NULL, FK -> `store.store_id` | 销售门店 |
| `customer_id` | FK -> `customer.customer_id`（可空） | 购买客户（可散客） |
| `sale_time` | NOT NULL | 销售时间 |
| `payment_method` | NOT NULL | 支付方式 |
| `total_amount` | NOT NULL, CHECK (`total_amount` >= 0) | 原始总额 |
| `discount_amount` | NOT NULL, DEFAULT 0, CHECK (`discount_amount` >= 0) | 优惠金额 |
| `actual_amount` | NOT NULL, CHECK (`actual_amount` >= 0) | 实付金额 |
---
## 3.15 `sale_item`（销售明细）
| 字段名 | 约束 | 说明 |
|---|---|---|
| `sale_id` | FK -> `sale.sale_id` | 所属销售单 |
| `line_no` | NOT NULL | 行号 |
| `product_id` | NOT NULL, FK -> `product.product_id` | 商品ID |
| `quantity` | NOT NULL, CHECK (`quantity` > 0) | 数量 |
| `unit_price` | NOT NULL, CHECK (`unit_price` > 0) | 成交单价快照 |
| `line_amount` | NOT NULL, CHECK (`line_amount` >= 0) | 行金额 |
| 复合主键 | PK (`sale_id`, `line_no`) | 明细唯一性 |
---
## 4. 关键约束汇总
| 约束类型 | 设计策略 |
|---|---|
| 主键约束 | 每表设置主键，子类表使用共享主键 |
| 外键约束 | 所有关联关系显式外键化 |
| 唯一约束 | ISBN、会员号、分类名等关键业务字段唯一 |
| 非空约束 | 业务必填字段统一 `NOT NULL` |
| 检查约束 | 价格>0、数量>0、金额/积分>=0 |
---
## 5. 命名规范（落地约定）
- 表名/字段名统一使用 `snake_case`
- 主键统一命名 `xxx_id`
- 外键字段与引用主键同名
- 中间表统一命名 `a_b`
- 时间字段统一 `*_time` 或 `created_at`
---
## 6. 第一阶段完成判定
满足以下条件即视为“表结构草案完成”：
- [x] 所有核心实体已落表；
- [x] 子类实体建模明确（`book`、`member`）；
- [x] 多对多关系已中间表化；
- [x] 主键/外键/唯一/非空/检查约束已定义；
- [x] 可直接进入 `create_tables.sql` 编写阶段。
