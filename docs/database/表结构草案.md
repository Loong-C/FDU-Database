# 网上综合书店销售数据库项目  
## 第一阶段：表结构草案（3NF）
> 文档用途：作为后续 `create_tables.sql` 的设计依据  
> 设计基础：与当前需求分析、ER 图保持一致
---
## 1. 设计原则
- 满足第三范式（3NF），减少冗余与更新异常  
- 主键统一使用 `xxx_id`  
- 外键字段名与被引用主键保持一致  
- 多对多关系通过中间表实现  
- 明确 `NOT NULL`、`UNIQUE`、`CHECK`、`FOREIGN KEY` 约束  
---
## 2. 表清单总览
| 模块 | 表名 | 说明 |
|---|---|---|
| 门店 | `store` | 门店信息 |
| 供应链 | `supplier` | 供应商信息 |
| 供应链 | `supplier_product` | 供货关系中间表 |
| 商品 | `category` | 商品分类 |
| 商品 | `product` | 商品父表（通用字段） |
| 图书 | `book` | 图书子表（专有字段） |
| 图书 | `publisher` | 出版社信息 |
| 图书 | `author` | 作者信息 |
| 图书 | `translator` | 译者信息 |
| 图书 | `book_author` | 图书-作者中间表 |
| 图书 | `book_translator` | 图书-译者中间表 |
| 客户 | `customer` | 客户信息 |
| 客户 | `member` | 会员子表 |
| 销售 | `sale` | 销售主单 |
| 销售 | `sale_item` | 销售明细 |
---
## 3. 表结构明细
## 3.1 `store`（门店）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `store_id` | BIGINT | PK | 门店主键 |
| `store_name` | VARCHAR(100) | NOT NULL, UNIQUE | 门店名称 |
| `city` | VARCHAR(50) | NOT NULL | 所在城市 |
| `address` | VARCHAR(255) | NOT NULL | 门店地址 |
| `phone` | VARCHAR(30) | UNIQUE | 联系电话 |
| `manager_name` | VARCHAR(50) |  | 门店负责人 |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
---
## 3.2 `supplier`（供应商）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `supplier_id` | BIGINT | PK | 供应商主键 |
| `supplier_name` | VARCHAR(100) | NOT NULL, UNIQUE | 供应商名称 |
| `contact_name` | VARCHAR(50) |  | 联系人 |
| `phone` | VARCHAR(30) |  | 联系电话 |
| `email` | VARCHAR(100) | UNIQUE | 邮箱 |
| `status` | VARCHAR(20) | NOT NULL | 合作状态（如 `active`/`inactive`） |
---
## 3.3 `category`（商品分类）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `category_id` | BIGINT | PK | 分类主键 |
| `category_name` | VARCHAR(100) | NOT NULL, UNIQUE | 分类名称 |
| `parent_category_id` | BIGINT | FK -> `category.category_id` | 父分类（可空） |
---
## 3.4 `product`（商品父表）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `product_id` | BIGINT | PK | 商品主键 |
| `product_name` | VARCHAR(200) | NOT NULL | 商品名称 |
| `category_id` | BIGINT | NOT NULL, FK -> `category.category_id` | 商品分类 |
| `unit` | VARCHAR(20) | NOT NULL | 计量单位（本/件/套等） |
| `unit_price` | DECIMAL(10,2) | NOT NULL, CHECK (`unit_price` > 0) | 零售价 |
| `cost_price` | DECIMAL(10,2) | CHECK (`cost_price` >= 0) | 成本价 |
| `stock_qty` | INT | NOT NULL, DEFAULT 0, CHECK (`stock_qty` >= 0) | 库存数量 |
| `barcode` | VARCHAR(50) | UNIQUE | 商品条码 |
| `status` | VARCHAR(20) | NOT NULL | 在售状态（如 `onsale`/`offsale`） |
| `created_at` | DATETIME | NOT NULL | 创建时间 |
---
## 3.5 `publisher`（出版社）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `publisher_id` | BIGINT | PK | 出版社主键 |
| `publisher_name` | VARCHAR(150) | NOT NULL, UNIQUE | 出版社名称 |
| `contact_name` | VARCHAR(50) |  | 联系人 |
| `phone` | VARCHAR(30) |  | 电话 |
| `email` | VARCHAR(100) |  | 邮箱 |
| `address` | VARCHAR(255) |  | 地址 |
| `country` | VARCHAR(50) |  | 国家/地区 |
| `website` | VARCHAR(255) |  | 官网 |
---
## 3.6 `book`（图书子表）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `product_id` | BIGINT | PK, FK -> `product.product_id` | 图书商品ID（与 `product` 1:1） |
| `isbn` | VARCHAR(20) | NOT NULL, UNIQUE | 国际标准书号 |
| `publisher_id` | BIGINT | NOT NULL, FK -> `publisher.publisher_id` | 出版社 |
| `publish_date` | DATE |  | 出版日期 |
| `edition` | VARCHAR(20) |  | 版次 |
| `language` | VARCHAR(30) |  | 语言 |
| `page_count` | INT | CHECK (`page_count` > 0) | 页数 |
---
## 3.7 `author`（作者）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `author_id` | BIGINT | PK | 作者主键 |
| `author_name` | VARCHAR(100) | NOT NULL | 作者姓名 |
| `country` | VARCHAR(50) |  | 国家/地区 |
---
## 3.8 `translator`（译者）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `translator_id` | BIGINT | PK | 译者主键 |
| `translator_name` | VARCHAR(100) | NOT NULL | 译者姓名 |
| `country` | VARCHAR(50) |  | 国家/地区 |
---
## 3.9 `book_author`（图书-作者中间表）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `product_id` | BIGINT | FK -> `book.product_id` | 图书ID |
| `author_id` | BIGINT | FK -> `author.author_id` | 作者ID |
| `author_order` | INT | CHECK (`author_order` > 0) | 作者顺序 |
| 复合主键 | - | PK (`product_id`, `author_id`) | 防止重复关联 |
---
## 3.10 `book_translator`（图书-译者中间表）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `product_id` | BIGINT | FK -> `book.product_id` | 图书ID |
| `translator_id` | BIGINT | FK -> `translator.translator_id` | 译者ID |
| 复合主键 | - | PK (`product_id`, `translator_id`) | 防止重复关联 |
---
## 3.11 `supplier_product`（供应商-商品中间表）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `supplier_id` | BIGINT | FK -> `supplier.supplier_id` | 供应商ID |
| `product_id` | BIGINT | FK -> `product.product_id` | 商品ID |
| `supply_price` | DECIMAL(10,2) | NOT NULL, CHECK (`supply_price` > 0) | 供货价 |
| `min_order_qty` | INT | CHECK (`min_order_qty` > 0) | 起订量 |
| `is_primary` | BOOLEAN | NOT NULL, DEFAULT FALSE | 是否主供 |
| 复合主键 | - | PK (`supplier_id`, `product_id`) | 防止重复供货关系 |
---
## 3.12 `customer`（客户）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `customer_id` | BIGINT | PK | 客户主键 |
| `customer_name` | VARCHAR(100) | NOT NULL | 客户姓名 |
| `phone` | VARCHAR(30) | UNIQUE | 手机号 |
| `email` | VARCHAR(100) | UNIQUE | 邮箱 |
| `address` | VARCHAR(255) |  | 地址 |
| `register_time` | DATETIME | NOT NULL | 注册时间 |
| `status` | VARCHAR(20) | NOT NULL | 客户状态 |
---
## 3.13 `member`（会员子表）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `customer_id` | BIGINT | PK, FK -> `customer.customer_id` | 会员客户ID |
| `member_no` | VARCHAR(50) | NOT NULL, UNIQUE | 会员编号 |
| `level` | VARCHAR(20) | NOT NULL | 会员等级 |
| `points` | INT | NOT NULL, DEFAULT 0, CHECK (`points` >= 0) | 积分 |
| `join_date` | DATE | NOT NULL | 入会日期 |
---
## 3.14 `sale`（销售主单）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `sale_id` | BIGINT | PK | 销售单主键 |
| `store_id` | BIGINT | NOT NULL, FK -> `store.store_id` | 销售门店 |
| `customer_id` | BIGINT | FK -> `customer.customer_id` | 购买客户（可空，支持散客） |
| `sale_time` | DATETIME | NOT NULL | 销售时间 |
| `payment_method` | VARCHAR(30) | NOT NULL | 支付方式 |
| `total_amount` | DECIMAL(12,2) | NOT NULL, CHECK (`total_amount` >= 0) | 原始总额 |
| `discount_amount` | DECIMAL(12,2) | NOT NULL, DEFAULT 0, CHECK (`discount_amount` >= 0) | 优惠金额 |
| `actual_amount` | DECIMAL(12,2) | NOT NULL, CHECK (`actual_amount` >= 0) | 实付金额 |
---
## 3.15 `sale_item`（销售明细）
| 字段名 | 建议类型 | 约束 | 说明 |
|---|---|---|---|
| `sale_id` | BIGINT | FK -> `sale.sale_id` | 所属销售单 |
| `line_no` | INT | NOT NULL | 行号 |
| `product_id` | BIGINT | NOT NULL, FK -> `product.product_id` | 商品ID |
| `quantity` | INT | NOT NULL, CHECK (`quantity` > 0) | 销售数量 |
| `unit_price` | DECIMAL(10,2) | NOT NULL, CHECK (`unit_price` > 0) | 成交单价快照 |
| `line_amount` | DECIMAL(12,2) | NOT NULL, CHECK (`line_amount` >= 0) | 行金额 |
| 复合主键 | - | PK (`sale_id`, `line_no`) | 明细唯一性 |
---
## 4. 约束汇总
| 约束类型 | 设计策略 |
|---|---|
| 主键约束 | 每表设置主键，子表使用共享主键（`book`、`member`） |
| 外键约束 | 实体关系全部外键化，防止孤儿数据 |
| 唯一约束 | ISBN、会员编号、分类名、门店名等关键字段唯一 |
| 非空约束 | 必填业务字段统一 `NOT NULL` |
| 检查约束 | 价格 `>0`、库存 `>=0`、销售数量 `>0`、金额/积分 `>=0` |
---
## 5. 命名规范
- 表名、字段名统一使用 `snake_case`  
- 主键统一命名 `xxx_id`  
- 外键字段名与引用主键同名  
- 中间表统一命名 `a_b`  
- 时间字段统一 `*_time` 或 `created_at`  
---
## 6. 阶段结论
当前表结构草案已覆盖需求分析与 ER 图中的核心实体、属性和关系，  
可直接进入第二阶段 SQL 脚本编写（`create_database.sql`、`create_tables.sql`、`insert_sample_data.sql`）。
