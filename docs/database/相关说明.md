# 第一阶段文档修改说明
> 说明：以下为本次对“需求分析”与“表结构草案”的具体调整记录，便于组内同步与复核。
---
## 一、需求分析修改内容
### 1) 门店 - 负责人
- **位置1**：`2.1 业务范围（In Scope）` 表格中“门店管理”行，数据关注点已写入“负责人”  
- **位置2**：`7. 关键属性清单` 中 `Store` 行，已写入 `manager_name`
### 2) 商品 - 计量单位
- **位置1**：`2.1 业务范围（In Scope）` 表格中“商品管理”行，已写入“计量单位”  
- **位置2**：`7. 关键属性清单` 中 `Product` 行，已写入 `unit`
### 3) 出版社 - 联系人、电话、email、地址
- **位置1**：`2.1 业务范围（In Scope）` 表格中“出版社管理”行，已写入“联系人、电话、邮箱、地址”  
- **位置2**：`7. 关键属性清单` 中 `Publisher` 行，已写入 `contact_name`、`phone`、`email`、`address`
### 4) 客户 - 地址
- **位置1**：`2.1 业务范围（In Scope）` 表格中“客户会员管理”行，已写入“地址”  
- **位置2**：`7. 关键属性清单` 中 `Customer` 行，已写入 `address`
### 5) 商品 - 库存（可加项）
- **位置1**：`2.1 业务范围（In Scope）` 表格中“商品管理”行，已写入“库存”  
- **位置2**：`6.2 数据一致性规则` 中已补充库存使用规则（记录可售数量、支持扣减/补货更新）  
- **位置3**：`6.3 合法值规则` 中已加入库存约束（`库存字段 >= 0`）  
- **位置4**：`7. 关键属性清单` 中 `Product` 行，已写入 `stock_qty`
---
## 二、表结构草案修改内容
### 1) 位置：`3.1 store`
- 新增字段：`manager_name`  
- 对应提出项：门店-负责人
### 2) 位置：`3.4 product`
- 新增字段：`unit`（计量单位）  
- 新增字段：`stock_qty`（库存数量，含 `DEFAULT 0`、`CHECK >= 0`）  
- 保留并明确：`status`（在售状态，如 `onsale/offsale`）  
- 对应提出项：商品-计量单位、商品-库存
### 3) 位置：`3.5 publisher`
- 新增字段：`contact_name`  
- 新增字段：`phone`  
- 新增字段：`email`  
- 新增字段：`address`  
- 对应提出项：出版社-联系人、电话、email、地址
### 4) 位置：`3.12 customer`
- 新增字段：`address`  
- 对应提出项：客户-地址
### 5) 位置：`3.2 supplier`
- `status` 字段保留，并在说明中明确为合作状态语义（合作/停用等）
### 6) 位置：`4. 约束汇总`
- 新增并统一强调库存约束：`库存 >= 0`  
- 保持原有约束逻辑：价格 `>0`、销售数量 `>0`、金额/积分 `>=0`
### 7) 位置：`2. 表清单总览（间接一致性调整）`
- 表结构未新增/删除表  
- 通过字段补充使其与需求分析、ER 图保持一致
---
## 三、当前状态结论
本次修改已完成组员提出的关键补充项，需求分析、ER 图与表结构草案之间已保持一致，可进入下一阶段 SQL 脚本细化。
